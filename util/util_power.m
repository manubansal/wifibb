
%----------------------------------------------------------------------------------------------------------------------------
function p = util_power(samples)
%----------------------------------------------------------------------------------------------------------------------------
  p = sum(samples.*conj(samples))/length(samples);
end

